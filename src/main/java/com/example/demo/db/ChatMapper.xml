<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="chat">

  <!-- 특정 닉네임으로 채팅 내용 불러오기-->
  <select id="listDM" resultType="ChatVO"> 	 	
  		select * from chat where ((from_id = ? and to_id=?)  order by dm_date
  </select>
  
  
</mapper>